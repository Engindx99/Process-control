mpc:
  prediction_horizon: 50
  control_horizon: 5
  setpoint: 1450.0
  weights:
    mse: 1.0
    smoothness: 484.0
    o2_tracking: 2000.0
    energy: 0.01

rl:
  total_timesteps: 100000
  learning_rate: 0.0003
  n_steps: 2048
  batch_size: 128
  gamma: 0.99
  action_limit: 0.1

simulation:
  initial_temp: 1400.0
  total_steps: 5000

hardware:
  num_cpu: 11
  device: "cpu"  # Veya "cuda"

paths:
  model_save_path: "models/ppo_kiln_residual_v1"